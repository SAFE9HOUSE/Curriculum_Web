# Серверная часть системы управления учебными планами

Разработка серверной части для модульной системы ведения, просмотра и поиска планов и дисциплин. Ведение версий планов, архивация устаревших планов.

## 🛠 Стек технологий

Для реализации серверной части был выбран современный стек технологий:

- **Java 21** — основной язык разработки с использованием новейших возможностей языка
- **Spring Boot 3.x** — фреймворк для быстрой разработки production-ready приложений
- **PostgreSQL 15+** — надежная реляционная СУБД для хранения структурированных данных направлений и учебных планов
- **Spring Security + JWT** — для аутентификации и авторизации пользователей
- **iText 7** — библиотека для генерации PDF-документов

## 🏗 Архитектурный подход

Проект построен по принципам RESTful MVC архитектуры с четким разделением на три слоя:

### 1. Слой контроллеров (Presentation Layer)
Реализованы REST-контроллеры (`@RestController`), которые:
- Обрабатывают HTTP-запросы (GET, POST, PUT, DELETE)
- Валидируют входные данные с помощью Spring Validation
- Возвращают структурированные ответы в формате JSON для API
- Предоставляют endpoint для экспорта учебных планов в PDF
- Обрабатывают исключения через глобальные обработчики (`@ControllerAdvice`)

### 2. Слой сервисов (Business Layer)
Содержит бизнес-логику приложения в классах, аннотированных `@Service`:
- Управление учебными планами, дисциплинами и преподавателями
- Генерация PDF-документов с поддержкой кириллицы через кастомные шрифты
- Транзакционное управление операциями с БД
- Кастомная валидация и обработка бизнес-исключений

### 3. Слой данных (Data Access Layer)
Использует гибридный подход для работы с данными:
- **Spring Data JPA** — для CRUD-операций и работы с сущностями
- **JDBC Template** — для выполнения сложных аналитических запросов
- **Кастомные репозитории** — для изоляции сложной SQL-логики
- **DTO паттерн** — для изоляции модели данных и контроля API-контракта

## ✨ Ключевые особенности реализации

- **Генерация PDF на русском языке** с поддержкой кириллических шрифтов
- **Стандартизированные ответы API** через обертку `SuccessResponse<T>`
- **Настроены CORS-политики** для безопасного межсайтового взаимодействия
- **Глобальная обработка ошибок** с возвратом структурированных JSON-ответов
- **JWT-аутентификация** с ролевой моделью (ADMIN, MANAGER)
- **Транзакционное управление** для обеспечения целостности данных
- **Кастомные исключения** для различных бизнес-сценариев
- **Оптимизация запросов** через комбинацию JPA и нативного SQL

### 🛡 Бизнес-ограничения и правила доступа

- **Направление подготовки** — заблокировано для изменений/удаления при наличии связанных учебных планов
- **Учебный план** — запрещено удаление при наличии контента (дисциплин)
- **Архивация** — возможность поместить план в архив (только роль `ADMIN`). Такой план закрыт для изменений/удаления
- **Просмотр данных** — любой пользователь имеет доступ ко всем `GET`-запросам (просмотр учебных планов, направлений, скачивание PDF)
- **Изменение данных** — ведение учебных планов требует аутентификации и соответствующих прав доступа
